#pragma once
#include <string>

class ScalarConverter{
    private:

    public:
        static void convert(std::string& value); // As a static method, you don't need to create an object to use
};